apiVersion: apps/v1
kind: Deployment
metadata:
  name: production-optimization-model
  labels:
    app: production-optimization-model
spec:
  selector:
    matchLabels:
      app: production-optimization-model
  replicas: 2
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: production-optimization-model
    spec:
      containers:
        - name: fastapi-production-optimization-model
          image: docker.io/skshreyas714/production-optimization:50
          imagePullPolicy: Always
          env:
            - name: ENABLE_METRICS
              value: "true"
            - name: METRICS_NAMESPACE
              value: "fastapi"
            - name: METRICS_SUBSYSTEM
              value: ""
          ports:
            - name: app
              containerPort: 80
          resources:
            requests:
              memory: 0.7Gi
              cpu: "0.25"
            limits:
              memory: 1Gi
              cpu: "0.5"
          livenessProbe:
            httpGet:
              path: /healthcheck
              port: 80

---
apiVersion: v1
kind: Service
metadata:
  name: production-optimization-model-service
  labels:
    app: production-optimization-model
spec:
  selector:
    app: production-optimization-model
  ports:
    - name: app
      port: 80
  type: ClusterIP

---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: production-optimization-model-servicemonitor
  # must by the same namespace that Prometheus is running in
  namespace: monitoring
  labels:
    app: production-optimization-model
    release: prometheus-stack
spec:
  selector:
    matchLabels:
      app: production-optimization-model
  endpoints:
    - path: metrics
      port: app
      interval: 15s
  namespaceSelector:
    any: true
